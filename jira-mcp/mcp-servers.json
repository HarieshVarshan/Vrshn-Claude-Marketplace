{
    "mcpServers": {
        "jira": {
            "type": "stdio",
            "command": "python",
            "args": ["${CLAUDE_PLUGIN_ROOT}/mcp_server.py"],
            "env": {
                "JIRA_CONFIG": "${HOME}/.config/atlassian/.env"
            }
        }
    }
}
