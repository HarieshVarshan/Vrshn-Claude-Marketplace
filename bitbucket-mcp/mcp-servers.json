{
    "mcpServers": {
        "bitbucket": {
            "type": "stdio",
            "command": "${HOME}/.local/share/bitbucket-mcp/venv/bin/python",
            "args": ["${CLAUDE_PLUGIN_ROOT}/mcp_server.py"],
            "env": {
                "BITBUCKET_CONFIG": "${HOME}/.config/atlassian/.env"
            }
        }
    }
}
