{
    "mcpServers": {
        "jenkins": {
            "type": "stdio",
            "command": "python",
            "args": ["${CLAUDE_PLUGIN_ROOT}/mcp_server.py"],
            "env": {
                "JENKINS_CONFIG": "${HOME}/.config/atlassian/.env"
            }
        }
    }
}
