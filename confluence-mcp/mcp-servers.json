{
    "mcpServers": {
        "confluence": {
            "type": "stdio",
            "command": "${HOME}/.local/share/confluence-mcp/venv/bin/python",
            "args": ["${CLAUDE_PLUGIN_ROOT}/mcp_server.py"],
            "env": {
                "CONFLUENCE_CONFIG": "${HOME}/.config/atlassian/.env"
            }
        }
    }
}
